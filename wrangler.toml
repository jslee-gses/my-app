# Cloudflare Pages reads the static files in this directory when deploying.
# No build step or server-side code is needed for this introductory app.
name = "my-app"
pages_build_output_dir = "./public"
compatibility_date = "2026-08-16"
